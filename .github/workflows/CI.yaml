name: CI
on:
  pull_request:
  workflow_dispatch:
permissions:
  checks: write
  pull-requests: write
  contents: read
  issues: write
jobs:
  ci:
    uses: HeyItsGilbert/.github/.github/workflows/ModuleCI.yml@main
