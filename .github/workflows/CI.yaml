name: Continuous Integration
on:
  pull_request:
  workflow_dispatch:
permissions:
  checks: write
  pull-requests: write
  contents: read
  issues: write
jobs:
  ci:
    name: Module CI
    uses: HeyItsGilbert/.github/.github/workflows/ModuleCI.yml@main
